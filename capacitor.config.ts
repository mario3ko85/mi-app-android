import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mario.miapp',
  appName: 'Mi App Android',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
